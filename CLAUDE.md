# Working agreements

- **Commits/pushes**: standing permission to commit and push without asking first, once tests pass (or for changes where tests don't apply, e.g. docs/config). Still report what was committed/pushed after the fact. This applies to this repo (`team-grade-processing/` and `team-grade-frontend/`).
